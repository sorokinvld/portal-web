import { Layout, Playground, Attributes } 
from 'lib/components' import { Text } from 'components'

export const meta = { title: 'Портал Агент', group: 'Ассистенты', index: 10, }

# Агенты

### 🚗 Как это работает

---

#### ⚠️ Отказ от ответственности!

###### АО Портал НЕ несет ответственность за использование пользователями любых опубликованных инструментов при любых обстоятельствах.

export default ({ children }) =&gt; &lt;Layout meta={meta}&gt;{children}&lt;/Layout&gt;